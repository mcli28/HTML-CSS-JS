var foo = "foo";
// Too compressed
function bar () {
	// Hard to know here things are.
	return true;
}

// Cleany delineated
function baz () {
	// Now return true
	return true;
}

// Bad
var = wheels;
wheels.clean()
car.apply(wheels);
truck.drive();

// Good
var = wheels;
wheels.clean()
car.apply(wheels);

truck.drive();
